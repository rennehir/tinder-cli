"use strict";
const React = require("react");
const { Box, Text } = require("ink");

const Profile = () => {
	return (
		<Box margin={3}>
			<Text>Profile</Text>
		</Box>
	);
};

module.exports = Profile;
